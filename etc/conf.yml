version: "v.001"

service:
  addr: "0.0.0.0:8080"

database:
  kind: "postgres" # local, postgres

  username: "executor_user"
  hostname: "db"
  port: 5432
  db: "executor"

executor:
  interpreter_path: "/bin/sh"
  sched_ticks: "5s"